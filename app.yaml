runtime: python310

entrypoint: gunicorn -w 4 -b :$PORT --timeout 120 app:app

env_variables:
  PYANNOTE_AUTH_TOKEN: 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

instance_class: F2

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 5

handlers:
  - url: /static
    static_dir: static

  - url: /.*
    script: auto
